#include<stdio.h>
int main()
{
    int a[]={1,2,3,4,5},x=5;
    for(int i=0;i<5;i++)
    {
        if(a[i]==x)
        {
            printf("%d element is found");
        }
        else
        {
            printf("element is not found");
        }
    }
    return 0;
}
